# Git-Identität für dieses Repo

**Immer so:** Alle Commits in diesem Repo sollen nur unter **Chris** (Account superentwickler) erscheinen.

- **Autor-Name:** Chris  
- **E-Mail:** superentwickler@users.noreply.github.com  

Lokal für dieses Repo setzen (bereits gesetzt):

```bash
git config user.name "Chris"
git config user.email "superentwickler@users.noreply.github.com"
```

Nach einem frischen Clone diese Zeilen einmal ausführen, damit keine andere Identität (z. B. Klarname) in die Contributors gerät.
