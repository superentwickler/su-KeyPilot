# KeyPilot – Example .env for Docker (project root, next to docker-compose.yml)
# Only for Docker: read by docker compose on start.
# backend/.env and frontend/.env are not used when running with Docker.
# Copy to .env and adjust: cp .env.example .env

# DB location on the host (default: ./backend/data)
# Absolute path allowed, e.g. for a custom folder or iCloud/OneDrive:
# KEYPILOT_DATA_DIR=/Users/yourname/Documents/Privat

# Optional ports (default: 80 for frontend, 8000 for backend)
# FRONTEND_PORT=80
# BACKEND_PORT=8000

# Ollama (local LLM) – only needed if Ollama runs on the host
# OLLAMA_BASE_URL=http://host.docker.internal:11434
# OLLAMA_MODEL=llama3.2
