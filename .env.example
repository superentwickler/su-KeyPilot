# KeyPilot – Example .env for Docker (project root, next to docker-compose.yml)
# Read by docker compose on start. backend/.env and frontend/.env are not used with Docker.
# Copy and adjust: cp .env.example .env
#
# --- Configuration (all optional) ---
#
# KEYPILOT_DATA_DIR  DB location on the host. Default: ./backend/data
#                    Can be an absolute path (e.g. custom folder, external drive).
# KEYPILOT_DATA_DIR=/Users/yourname/Data/KeyPilot
#
# FRONTEND_PORT      Port for frontend (default: 80). Then open http://localhost:<port>
# BACKEND_PORT       Port for backend API (default: 8000)
#
# OLLAMA_BASE_URL    Ollama API when Ollama runs on the host (Mac/Windows: host.docker.internal)
# OLLAMA_MODEL      Model name
# OLLAMA_BASE_URL=http://host.docker.internal:11434
# OLLAMA_MODEL=llama3.2
