# KeyPilot Frontend – Example .env
# Only for local runs (npm run dev / ./scripts/start-local.sh frontend).
# When using Docker, these values are not used – port is set via project root .env (FRONTEND_PORT).
# Copy to .env and adjust if needed: cp .env.example .env

# Dev server port (default: 5173)
FRONTEND_PORT=5173
