# KeyPilot Frontend – Example .env (local runs only)
# Used by: npm run dev, ./scripts/start-local.sh frontend
# Not used with Docker (see project root .env for FRONTEND_PORT).
# Copy and adjust: cp .env.example .env
#
# FRONTEND_PORT  Dev server port (default: 5173)

FRONTEND_PORT=5173
