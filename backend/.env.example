# KeyPilot Backend – Example .env (local runs only)
# Used by: uvicorn, ./scripts/start-local.sh backend
# Not used when running with Docker (see project root .env).
# Copy and adjust: cp .env.example .env
#
# --- Configuration (all optional) ---
#
# KEYPILOT_DATA_DIR  Directory for keypilot.db. Unset = backend/data.
#                    Local runs: only paths inside the project (e.g. backend/data, backend/mydata).
#                    Paths outside the project are ignored and backend/data is used.
# KEYPILOT_DATA_DIR=backend/data
#
# OLLAMA_BASE_URL    Ollama API (local LLM). Default: http://localhost:11434
# OLLAMA_MODEL       Model name, e.g. llama3.2, qwen2.5:7b
# DEBUG              Set to true for verbose logs

OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2
DEBUG=false
